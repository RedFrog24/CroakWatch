# Local-only files - never ship to the RedGuides-linked repo.
# Only init.lua (+ README + any runtime assets) + the publish workflow should be tracked.
*.md
!README.md
backup/
.claude/
tools/
.luacheckrc
